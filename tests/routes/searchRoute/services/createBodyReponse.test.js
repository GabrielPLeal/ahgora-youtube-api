describe('Test Create Body Response Function', () => {
    // We aren't do request to youtube api on the tests. 
    // Need mock the response data.
    // If too many requests are made, the day quota limit is reached.
    it('Pass query should return json body reponse', async () => {
        // const videosData = mockVideosData(3)
        // const bodyResponse = creatBodyResponse(videosData)
        expect(true).toBeTruthy()
    })
})